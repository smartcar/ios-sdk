language: objective-c
osx_image: xcode9.3
xcode_workspace: SmartcarAuth.xcworkspace
xcode_scheme: SmartcarAuth

deploy:

  - provider: script
    skip_cleanup: true
    script: echo "adding authorization"

  - provider: script
    skip_cleanup: true
    script: pod trunk push SmartcarAuth.podspec
    on:
      branch: master
